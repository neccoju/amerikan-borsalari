# Exported core runtime dependencies (mirror of [project].dependencies in pyproject.toml).
# The canonical source of truth is pyproject.toml. Regenerate with:
#   pip-compile  (or)  pip freeze | grep -E '...'
#
# Optional extras (install only when the matching phase/keys are enabled):
#   pip install -e ".[llm]"    anthropic, openai
#   pip install -e ".[macro]"  fredapi
#   pip install -e ".[news]"   finnhub-python, alpha_vantage
#   pip install -e ".[dev]"    pytest, ruff

pandas>=2.0
numpy>=1.24
yfinance>=0.2.40
pandas-market-calendars>=4.3
PyYAML>=6.0
Jinja2>=3.1
requests>=2.31
vaderSentiment>=3.3
